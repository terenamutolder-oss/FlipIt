{
  "name": "flipit",
  "version": "1.0.0",
  "description": "FlipIt - Master Your Memory",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "web": "npx --yes serve . -l 3000",
    "cap:sync": "node scripts/copy-www.js && npx cap sync",
    "cap:android": "npx cap open android",
    "build:mobile": "node scripts/copy-www.js && npx cap sync"
  },
  "keywords": ["flashcard", "study", "electron", "capacitor", "web"],
  "author": "FlipIt Team",
  "license": "ISC",
  "capacitor": {
    "platforms": ["android"]
  },
  "devDependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "electron": "^28.0.0"
  }
}
